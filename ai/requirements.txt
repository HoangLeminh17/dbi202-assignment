pyodbc>=5.0
pandas>=2.0
scikit-learn>=1.3

# NL2SQL Agent (ai/nl2sql/) - xem ai/NL2SQL_ARCHITECTURE.md
sqlglot>=25.0
python-dotenv>=1.0
flask>=3.0   # web demo: ai/nl2sql/webapp.py
# Chỉ cần cài SDK của provider đang dùng (chọn qua LLM_PROVIDER trong .env):
anthropic>=0.40   # LLM_PROVIDER=anthropic
# openai>=1.50    # LLM_PROVIDER=openai
# google-generativeai>=0.8  # LLM_PROVIDER=gemini

pytest>=8.0   # chạy: pytest ai/nl2sql/tests/
